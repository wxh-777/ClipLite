# ClipLite 使用说明（简单易懂版）

本文适用于 ClipLite **1.2.5 x64**。

ClipLite 可以记住你复制过的内容。以后想再次使用时，不用回到原来的网页或文件夹，直接从历史记录里找到并粘贴即可。

## 一、最简单的用法

只需要记住下面三步：

1. 像平时一样按 `Ctrl+C` 复制文字、图片或文件。
2. 按 `Alt+V` 打开 ClipLite 历史记录。
3. 点击想要的内容，ClipLite 会自动把它粘贴到刚才使用的程序里。

ClipLite 会在电脑右下角的托盘区域运行。托盘区域通常在时间和网络图标附近。如果暂时看不到 ClipLite 图标，请点击托盘区域旁边的向上箭头。Explorer 重启或任务栏刷新后，ClipLite 会自动恢复托盘图标。

## 二、安装和第一次打开

### 安装版

1. 双击安装文件，例如 `ClipLite-Setup-1.2.5-x64.exe`。
2. 一直点击安装向导中的下一步即可。
3. 安装完成后打开 ClipLite。

安装版可以在 Windows 的“应用和功能”中卸载。卸载时默认保留全部用户数据；如需完全删除，请在卸载向导中勾选“删除 ClipLite 的全部用户数据”。

### 免安装版

免安装版通常是一个 `.zip` 压缩包：

1. 右键压缩包，选择“全部解压”。
2. 打开解压后的文件夹。
3. 双击 `ClipLite.exe`。

不要只把 `ClipLite.exe` 单独拿出来。整个文件夹都要保留。免安装版适合放在 U 盘中使用。

### 第一次启动后

ClipLite 会在后台记住之后复制的内容。已经复制过、但没有重新复制的内容不会自动补回来；请重新按一次 `Ctrl+C`。

默认情况下，第一次启动可能会打开设置窗口。设置完成后，关闭设置窗口即可。以后不想每次启动都看到设置窗口，可以关闭“启动后打开设置”。

## 三、如何复制和找回内容

### 复制内容

ClipLite 可以记住：

- 普通文字；
- 网页上的文字和格式；
- 图片；
- 文件或文件夹。

复制操作仍然由原来的程序完成，ClipLite 只负责帮你保存一份历史记录。

### 打开历史记录

最方便的方法是按 `Alt+V`。

也可以：

- 右键单击右下角的 ClipLite 图标，选择“打开历史”；
- 双击 `ClipLite.exe`，如果 ClipLite 已经运行，它会打开已有程序的历史窗口。

### 粘贴一条记录

1. 按 `Alt+V` 打开历史窗口。
2. 找到要使用的内容。
3. 点击这条内容。

ClipLite 会自动完成粘贴。历史窗口通常会自动关闭。

如果不方便使用鼠标，也可以用键盘：

- `Up` 和 `Down`：选择上一条或下一条；
- `Enter`：粘贴选中的内容；
- `Esc`：关闭历史窗口。

### 粘贴到哪里

ClipLite 会记住你打开历史窗口前正在使用的程序，并尝试粘贴回那里。例如，你正在 Word 中输入文字，按 `Alt+V` 找到历史记录后点击一条内容，它会回到 Word 中粘贴。

如果原来的程序已经关闭，或者原来的输入框已经不存在，粘贴可能失败。重新点击目标程序的输入框，再打开历史记录试一次。

## 四、历史窗口怎么用

### 搜索

打开历史窗口后，直接在上方的搜索框输入关键词即可。例如搜索“地址”“会议”或一段文字。

搜索不区分英文字母大小写，也支持中文。

如果输入的文字跑到了原来的程序里：

1. 打开设置。
2. 打开“搜索输入兼容模式”。
3. 回到历史窗口，按 `Ctrl+Space` 后再输入搜索内容。

搜索图片时，ClipLite 不能根据图片里面的文字或画面搜索；图片只能通过来源程序等信息找到。

### 只看某一种内容

历史窗口中的筛选按钮可以只显示：

- 全部内容；
- 已经置顶的内容；
- 文字；
- 图片；
- 文件。

点击“筛选”还可以按下面的条件查找：

- 最近一天、前一天、最近七天或更早的内容；
- 短文本、中等文本或长文本；
- 哪个程序复制的内容；
- 最新的、最早的、按程序排列或按内容长度排列。

多个条件可以一起使用。例如先选择“图片”，再选择“最近七天”，就只会显示最近七天复制的图片。

按 `Ctrl+0` 可以清除筛选条件。点击搜索框旁边的清除按钮，可以清除搜索文字。

### 置顶

重要内容可以置顶：

1. 在历史窗口中找到这条内容。
2. 点击右侧的置顶按钮。

置顶后，这条内容不会因为自动清理而消失。再次点击可以取消置顶。

也可以右键单击历史窗口的空白位置，选择“置顶历史窗口”，让历史窗口一直显示在其他窗口上面。再次选择可以取消置顶。

### 删除

- 点击记录右侧的删除按钮，可以删除一条记录；
- 也可以右键单击记录，选择“删除”；
- 选中记录后按 `Delete`，也可以删除。

删除后不能恢复，请确认后再操作。即使记录已经置顶，也可以手动删除；置顶只防止自动清理。

## 五、图片、长文字和文件预览

ClipLite 有一个单独的预览窗口，可以先看内容，再决定是否粘贴。

### 选择预览方式

在设置中，或者在历史记录上右键单击后选择“预览模式”：

- **自动预览**：鼠标停在记录上时自动显示预览；
- **按住按键预览**：默认按住 `F2` 时显示预览，松开后关闭；
- **关闭预览**：完全不显示预览窗口。

### 预览窗口的操作

- 长文字：在预览窗口中滚动鼠标滚轮；
- 图片：把鼠标放在想查看的位置，滚动滚轮放大或缩小；
- 放大的图片：按住鼠标左键并拖动，可以查看图片的其他部分；
- 文件：如果文件本身是图片，可能会显示缩略图，但实际保存的仍然是文件本身。

如果文件被移动或删除，ClipLite 会提示文件已经不存在。请在原来的文件位置找到文件后重新复制。

## 六、纯文字和带格式文字

普通点击会按照原来的样子粘贴：

- 普通文字仍是普通文字；
- 网页或富文本会尽量保留格式；
- 图片仍作为图片粘贴；
- 文件仍作为文件粘贴。

对一条记录右键单击，还可以选择：

- **粘贴为纯文本**：去掉字体、颜色等格式，只留下文字；
- **粘贴为富文本**：尽量保留网页或文字的原有格式。

“粘贴为富文本”只对网页或富文本记录可用。

## 七、常用快捷键

| 按键 | 用途 |
| --- | --- |
| `Alt+V` | 打开历史记录 |
| `Enter` | 粘贴选中的记录 |
| `Ctrl+Shift+V` | 粘贴为纯文本 |
| `Ctrl+Shift+R` | 粘贴为富文本 |
| `Delete` | 删除选中的记录 |
| `Esc` | 关闭历史窗口 |
| `F10` | 打开设置 |
| `Ctrl+0` | 清除筛选条件 |
| `F2` | 按住预览当前记录 |
| `Up` / `Down` | 选择上一条或下一条 |
| `Page Up` / `Page Down` | 上下翻页 |
| `Home` / `End` | 跳到第一条或最后一条 |

### 修改快捷键

1. 打开设置，进入“快捷键”。
2. 点击要修改的按键框。
3. 直接按下新的按键组合。

打开历史、打开设置、暂停监听这三个全局快捷键，至少要带一个 `Ctrl`、`Alt`、`Shift` 或 `Win`。如果提示快捷键已经被占用，请换一个组合。

## 八、设置怎么选

设置会自动保存，不需要寻找“保存”按钮。修改输入框后等一小会儿，或者直接关闭设置窗口即可。

### 外观和使用方式

- **主题**：选择跟随 Windows、浅色或深色；
- **语言**：选择跟随系统、中文或 English；
- **强调色**：改变按钮和选中内容的颜色；
- **搜索输入兼容模式**：解决搜索文字输入到其他程序的问题；
- **粘贴后移到首位**：打开后，刚刚粘贴过的内容会排到最前面；
- **自动预览**：鼠标停留时自动查看内容；
- **按住按键预览**：按住 `F2` 查看内容。

### 电脑启动和监听

- **暂停剪贴板监听**：打开后，ClipLite 暂时不再记住新的复制内容；以前保存的内容不受影响；
- **随 Windows 启动**：开机登录后自动运行 ClipLite；
- **启动后打开设置**：每次启动时显示设置窗口；
- **启动后显示系统提示**：启动时显示一条通知；
- **以管理员权限运行**：让 ClipLite 用更高权限运行，通常只有在需要向管理员程序粘贴时才需要尝试。

### Win + V

打开“强制替换 Win + V”后，按 `Win+V` 会尝试打开 ClipLite 历史窗口。

Windows 或其他程序可能已经占用了 `Win+V`。如果这个功能没有生效，请使用默认的 `Alt+V`，或者换一个历史快捷键。

### 历史保存多少内容

在“存储管理”中可以限制历史大小：

- **最大记录数**：最多保存多少条；填 `0` 表示不限制数量；
- **保留天数**：只保留最近多少天；填 `0` 表示不按天数清理；
- **最大磁盘空间**：最多占用多少 MB；填 `0` 表示不限制空间；
- **单条内容上限**：一条内容最多允许多大，最大为 `32 MB`；超过这个大小的内容不会保存；
- **缓存目录**：把历史文件放到其他文件夹。

重要内容请先置顶。自动清理会保留置顶内容。

文字、图片和文件还可以分别设置数量和空间上限。设置得更小后，程序可能会删除已经存在的未置顶记录，操作前会先询问你。

### 手动清理

在“存储管理”中可以清理：

- 所有未置顶历史；
- 未置顶文字；
- 未置顶图片；
- 未置顶文件。

程序会先告诉你准备删除多少条，确认后才会删除。置顶记录会保留。

## 九、隐私和安全

### 历史保存在哪里

普通安装版默认保存到：

```text
%LOCALAPPDATA%\ClipLite
```

其中主要文件是：

- `history.bin`：历史内容；
- `settings.ini`：设置；
- `cliplite.log`：诊断日志。

免安装版默认把这些内容放在程序文件夹的 `data` 文件夹中。

### 保护历史内容

在“安全与隐私”中打开“使用 Windows 用户加密保护历史”，可以让历史内容只能由当前 Windows 用户读取。

这项功能适合共用电脑的情况，但也要注意：换 Windows 用户、换电脑或重装系统后，可能无法读取这些加密历史。打开或关闭这项功能时，程序需要处理全部旧记录，大图片较多时请耐心等待，不要强行结束程序。

### 不保存某些程序的复制内容

在“忽略应用”中，每行写一个程序名称。写入后，从这些程序复制的内容不会保存。

如果不确定名称，可以先在历史记录上查看来源名称，再把它逐行填入。

### 让敏感内容自动消失

在“敏感内容过期时间”中填写小时数，例如 `2` 表示两小时后自动删除被识别为敏感的文字。填写 `0` 表示关闭。

ClipLite 会重点识别包含 `password`、`token`、`api_key`、`secret` 或私钥提示的文字。它不是绝对可靠的安全检查；密码、验证码和密钥仍然不建议长时间留在剪贴板中。

## 十、换位置、备份和恢复

### 修改历史保存位置

1. 打开设置，进入“存储管理”。
2. 在“缓存目录”中输入新的文件夹，或点击“浏览”。
3. 等待迁移完成。

新文件夹必须可以写入，而且最好是空文件夹。如果里面已经有其他 ClipLite 历史，程序可能会拒绝迁移，以免覆盖旧内容。

设置文件的位置不会因为“缓存目录”改变而改变。

### 备份

1. 先从托盘菜单退出 ClipLite。
2. 复制默认数据文件夹中的 `settings.ini`。
3. 复制历史所在文件夹中的 `history.bin`。
4. 把它们放到 U 盘或其他安全位置。

如果使用了自定义缓存目录，还要一起备份那个目录中的历史文件和缩略图缓存。

恢复时先退出 ClipLite，再把备份文件放回原来的位置。加密历史只能在原来的 Windows 用户环境中恢复。

## 十一、遇到问题怎么办

### 复制后没有出现在历史里

请按顺序检查：

1. 右下角是否还有 ClipLite 图标；
2. “暂停剪贴板监听”是否关闭；
3. 是否把这个程序写进了“忽略应用”；
4. 内容是否太大，超过“单条内容上限”；
5. 保存历史的文件夹是否还存在并且可以写入；
6. 复制后稍等片刻，再按 `Alt+V` 查看。

### 点击后没有粘贴

请确认：

- 目标程序还没有关闭；
- 目标程序中确实点开了可以输入文字的位置；
- 没有一直按住 `Ctrl`、`Alt`、`Shift` 或 `Win`；
- 如果目标程序是“以管理员身份运行”，ClipLite 也尝试以管理员身份运行。

Windows 可能禁止普通程序向管理员程序、登录界面或安全界面自动粘贴。这不是 ClipLite 可以绕过的限制。也可以先在目标程序中点一下输入框，再手动按 `Ctrl+V`。

### 搜索内容跑到别的程序

打开“搜索输入兼容模式”，再按 `Ctrl+Space` 后输入。仍然不行时，可以先用鼠标点击搜索框，再输入关键词。

### 富文本选项不能点击

只有网页或带格式的文字才支持“粘贴为富文本”。普通文字、图片和文件没有这个选项。

### 图片或文件缩略图没有显示

大图片第一次显示时可能需要等待。文件被移动、删除或无法读取时不会显示缩略图；请回到原位置重新复制文件。

### 历史窗口一闪就不见了

这是默认行为：历史窗口失去焦点后会自动关闭。需要一直显示时，打开历史窗口后点击置顶按钮，或右键选择“置顶历史窗口”。

### Win + V 没有反应

确认“强制替换 Win + V”已经打开，然后重新启动 ClipLite。如果仍然无效，请改用 `Alt+V`。有些 Windows 设置或其他程序会占用 `Win+V`。

### 程序启动失败

在设置中点击“打开日志文件”，或者打开下面的文件：

```text
%LOCALAPPDATA%\ClipLite\cliplite.log
```

日志不会记录剪贴板正文，但可能包含文件路径和程序名称。把日志发给别人前，请先删除自己的用户名、文件路径和其他隐私信息。

## 十二、给熟悉电脑的用户

可以在命令行中使用：

```powershell
.\ClipLite.exe --history
.\ClipLite.exe --settings
.\ClipLite.exe --exit
```

- `--history`：打开历史窗口；
- `--settings`：打开设置窗口；
- `--exit`：退出已经运行的 ClipLite。

ClipLite 只允许同时运行一个实例。再次运行程序不会打开第二个后台程序，而是通知原来的程序执行操作。

## 十三、需要注意的事情

- ClipLite 只支持 Windows x64；
- 只会记住启动以后发生的复制操作；
- 删除、清理和自动过期后的内容不能恢复；
- 文件记录保存的是文件位置，文件被移动或删除后，ClipLite 不能找回文件本身；
- 普通点击删除可以删除置顶记录，置顶只防止自动清理；
- ClipLite 不能绕过 Windows 的权限限制；
- 使用加密后，请务必记住当前 Windows 用户和备份位置。

反馈问题时，请说明 Windows 版本、ClipLite 版本、安装版还是免安装版、具体操作步骤，以及是否打开了管理员权限、Win + V 替换或历史加密。

安全问题请阅读 [SECURITY.md](SECURITY.md)，许可证和商业使用限制请阅读 [LICENSE.md](LICENSE.md)。
