# 更新日志

## [1.2.0] - 2026-09-10

- 图片预览支持按住鼠标左键拖拽查看放大后的原图。
- 预览窗口支持长文本滚轮滚动，以及以鼠标所在位置为中心的原图放大缩小；缩放内容由原始图片重新渲染，不放大列表缩略图。
- 设置窗口内容较长时增加右侧可拖动滚动条，并支持滚轮和轨道分页。
- 优化设置页滚动刷新范围，减少父窗口和未变化子控件的重复重绘，降低滚动闪烁与布局计算开销。
- 修复设置控件滚动到视口边缘时被反复隐藏和显示的问题，改为保留控件并裁剪超出内容区域的部分。
- 设置窗口启用窗口级合成绘制，避免输入框、开关和按钮在滚动重排时分批显示或消失。
- 增加固定标题区覆盖层，确保滚动内容不会绘制到标题栏区域。
- 修复分类存储设置重启后被错误映射的问题，并在应用退出时立即保存仍打开的设置窗口修改。
- 修复新增剪贴板记录后分类空间和全局存储上限未即时执行的问题。
- 设置窗口存储管理中的主动清除仅删除未置顶记录，置顶记录会保留并在确认及结果文案中明确说明。
- 优化历史窗口单条删除，避免同步重新处理所有历史内容，减少大型图片历史删除时的界面卡顿。
- 修复鼠标停留在删除按钮位置连续删除时，列表刷新后的第二次点击可能误关闭历史窗口的问题。
- 修复历史列表删除记录后刷新列表回到顶部的问题，现在会保留当前滚动位置。
- 扩大历史列表删除图标的悬停和点击命中范围，降低点击精度要求。
- 修复按住预览键再次预览同一条记录时只显示空预览窗口的问题；释放预览后会正确清理记录状态并重新加载内容。
- 新增独立原图预览浮窗：设置页和历史记录右键菜单支持自动预览、按键预览和关闭预览；按键预览默认使用 `F2`，不会改变历史窗口布局或单击粘贴行为。
- 图片详情预览按需读取历史中的原始 DIB/DIBV5 或文件原图，不复用列表缩略图缓存；后台任务过期时会丢弃结果并释放资源。
- 优化图片列表滚动时的加载显示，加载期间保留固定尺寸的抗锯齿占位区域，不再先显示 `[图片]` 文字再切换为缩略图。
- 修复滚动时旧代次图片任务占满队列，导致当前视口缩略图未能重新入队并长期显示 `[图片]` 的问题。
- 修复缩略图内存缓存达到上限时淘汰仍在加载的条目，导致后台完成后图片结果被丢弃的问题。
- 修复历史列表滚动预加载与后台缩略图写入同时访问缓存索引时的并发问题，避免滚动一段距离后图片缩略图不显示。
- 修复后台 DIB 缩放返回空位图时仍被视为成功，导致历史列表长期显示图片文字而不显示缩略图的问题。
- 修复滚动后图片缩略图任务被旧代次或失败状态阻塞的问题；当前视口任务优先，失败记录会在后续滚动中重试。
- 新增受限的磁盘缩略图缓存：缩略图与历史数据分离保存，内存使用 LRU 缓存，磁盘缓存上限为 32 MB/5000 项；启用历史加密时缩略图同步使用 DPAPI。
- 将历史图片缩略图内存缓存上限从 12 项调整为 24 项，减少长列表来回滚动时的重复加载。
- 修复快速滚动时已缓存图片缩略图被临时隐藏，导致图片闪烁的问题；现在只延后未缓存图片的加载。
- 优化快速滚动历史列表时的图片预览：滚动期间跳过新图片解码，停止滚动后加载当前视口及前后一屏的文件图片，减少大图读取、缩放和后台任务堆积。
- 修复首次唤起历史窗口时首帧尚未绘制导致短暂出现透明空窗口的问题。
- 文件列表中的图片文件现在按需显示缩略图；仍按文件列表保存和粘贴，文件不存在或无法解码时回退显示路径。
- 文件列表中的路径失效时，历史项显示“文件已不存在”标记；粘贴前会检查所有路径并弹出明确提示，避免无响应反馈。
- 修复设置页多行设置项在部分 DPI 和字体环境下第二行文字显示不完整的问题。
- 修复快捷键粘贴等待修饰键释放时阻塞界面消息线程，导致粘贴延迟以及粘贴后立即输入卡顿的问题；现在改为异步等待并连续发送粘贴按键。

### 构建与验证

- 统一程序、资源、CMake、安装器、打包脚本和中英文 README 版本为 `1.2.0`。
- x64 Release 构建、`ClipLiteStore` CTest、历史窗口压力测试 `100/100` 和发布包生成通过；当前 Release 基线为主程序 `658 KB`、启动耗时 `287.56 ms`、Working Set `13.69 MB`、Private Bytes `2.60 MB`、GDI `13`、USER `10`。

## [1.1.0] - 2026-09-03

- 历史窗口条目和空白区域的右键菜单增加“粘贴后移到首位”开关，点击后立即保存并更新列表排序。
- 修复历史列表右键菜单中的筛选入口仍使用旧菜单的问题，现在统一打开自绘筛选菜单。
- 将“粘贴后移到首位”从历史窗口快捷键卡片移至常规设置卡片。
- 优化历史窗口头部布局，扩大筛选入口并将条件数量改为独立角标，避免高 DPI 下搜索框和操作按钮拥挤或文字截断。
- 来源应用筛选列表增加当前运行应用的系统缩略图标，并在历史窗口关闭时释放图标缓存。
- 修复筛选一级、二级菜单四角露出矩形底色的问题，菜单窗口现在会按圆角区域裁剪。
- 修复筛选菜单选择条件后立即收起的问题，现在可以连续选择多个筛选条件，点击菜单外或按 Esc 才会关闭菜单。
- 筛选按钮改用双缓冲自绘浮动菜单，减少操作闪烁；一级条件点击后保持显示并展开二级菜单，来源应用列表支持滚轮、滚动条和拖动。
- 修复筛选菜单及来源应用子菜单位于历史窗口外时，被全局鼠标钩子误判为外部点击并关闭历史窗口的问题。
- 修复每次启动无条件重建历史文件导致大体积历史启动缓慢的问题；正常历史只做校验扫描，尾部损坏时直接截断到最后一条有效记录。
- 参考筛选菜单原型扩展历史窗口筛选：增加时间范围、内容长度、来源应用多选和排序方式，并显示当前条件数量与命中记录数；关键词、分类和菜单条件取交集。
- 修复部分应用复制后延迟提供剪贴板格式时，剪贴板捕获阻塞主消息线程，导致粘贴、切换窗口和快捷键响应延迟数秒的问题。
- 剪贴板内容捕获改为后台任务，连续快速复制时合并后续通知，不再阻塞历史窗口和全局快捷键。
- 设置页新增“粘贴后移到首位”，开启后按最近使用活动排序，关闭时继续按最近复制时间排序。
- v4 历史记录增加稳定记录 ID、首次录入时间、毫秒级最近复制时间、最后使用时间、复制次数、使用次数和逻辑内容长度元数据，为后续排序、长度与使用频率筛选提供基础。
- 元数据区增加版本、长度和 CRC 校验，保留对先前 v4 元数据布局的读取与自动升级能力。
- 修复使用统计的强制磁盘提交和重复复制整库重建造成的快速粘贴延迟；相同内容现在优先原位更新元数据。

### 构建与验证

- 统一程序、资源、CMake、安装器、打包脚本和中英文 README 版本为 `1.1.0`。
- x64 Release 构建、`ClipLiteStore` CTest 和资源测量通过；当前 Release 基线为主程序 `587.5 KB`、启动耗时 `53.03 ms`、Working Set `13.73 MB`、Private Bytes `2.50 MB`、GDI `13`、USER `10`。

## [1.0.5] - 2026-08-26

修复历史窗口搜索输入焦点问题，并增加中文输入法兼容模式设置。

### 修复与体验

- 普通搜索默认点击后直接获得输入焦点，避免搜索文字进入原应用。
- 修复搜索输入被误判为外部操作而关闭历史窗口的问题。
- 兼容模式下保留原应用前台，使用 `Ctrl+Space` 临时进入真实输入法焦点，且切换快捷键的空格不会写入搜索框。
- 设置页新增“搜索输入兼容模式”开关，默认关闭并自动保存。
- 历史窗口宽度调整为 `400px`，缩小分类栏与历史列表间距，并保留更多剪贴板预览空间。
- 文本和 HTML 历史预览上限从 `160` 字节提高到 `256` 字节。
- 保留文本预览中的换行和制表符，并修复运行期间与重启后预览格式不一致的问题。

### 构建与验证

- 统一程序、资源、CMake、安装器、打包脚本和中英文 README 版本为 `1.0.5`。
- x64 Release 构建、`ClipLiteStore` CTest 和历史窗口压力测试 `100/100` 通过。
- 最新 Release 实测：主程序 `549.5 KB`、启动 `56.12 ms`、Working Set `13.51 MB`、Private Bytes `2.73 MB`、GDI `13`、USER `10`。

## [1.0.4] - 2026-08-25

精简安装包和便携包中的文档资源，界面截图改由 GitHub 按版本托管。

### 发布调整

- 中英文 README 的界面截图改为固定的 `v1.0.4` GitHub Raw 地址。
- 安装包和便携包不再复制 `docs/screenshots`，保留仓库中的原图作为线上来源。
- 打包脚本增加当前用户目录下 Inno Setup 的自动检测。
- 统一程序、Windows 文件资源、构建配置、安装器、打包脚本和中英文 README 版本为 `1.0.4`。

## [1.0.3] - 2026-08-25

完善设置窗口、历史加密提示和支持作者功能，并精简发布包中的本地资源。

### 功能与体验

- 设置窗口增加最小化能力，支持再次点击任务栏图标最小化，同时保持窗口不可缩放和不可最大化。
- Windows 用户加密开关增加 800 毫秒防抖，连续切换只迁移一次最终状态，关闭设置窗口时立即提交。
- 隐私设置增加迁移、跨用户/设备解密限制及大型图片可能影响剪贴板和列表滚动性能的中英文提醒。
- 支持作者和 QQ 群窗口改由独立辅助进程运行，关闭设置窗口或主程序时同步回收。
- 支持图片改为联网异步加载，移除安装包和便携包中的本地支付及 QQ 群图片资源。
- 历史列表滚轮输入改为直接更新滚动位置，移除不再使用的滚动补间定时器。

### 构建与验证

- 统一 CMake、Windows 文件资源、安装器、打包脚本、关于页和中英文 README 版本为 `1.0.3`。
- 更新中英文界面截图，并增强 MSVC Release 的函数级链接优化和无增量链接配置。
- x64 Debug、Release 构建和隔离 `ClipLiteStore` CTest 通过。

## [1.0.2] - 2026-08-24

修复历史窗口夺取输入焦点的问题，使粘贴交互接近 Windows 系统剪贴板行为。

修复 UTF-8 文本预览在字符边界截断时显示为无效文本的问题；完整文本和粘贴内容不受影响。

### 修复与验证

- 历史窗口改为非激活窗口，原应用输入框保留系统焦点和光标状态。
- 通过临时键盘钩子将上下、翻页、回车和 Esc 等列表控制键交给历史窗口处理。
- 用户直接输入普通字符时关闭历史窗口，并让字符继续进入原应用输入框。
- 移除 UI Automation 文本改写和不再使用的 `uiautomationcore` 链接，保留标准 Ctrl+V 粘贴路径。
- x64 Release 构建和隔离 `ClipLiteStore` CTest 通过。

## [1.0.1] - 2026-08-24

修复一次剪贴板变更被分阶段通知时产生重复文本记录的问题，并强化异常剪贴板数据、存储失败路径和搜索线程的生命周期处理。

### 修复与验证

- 历史窗口缓存最近显示的图片缩略图，滚动时避免重复读取和解码原图；大图 CRC 校验改为查表实现。
- 新增自包含图片滚动基准，使用进程专属临时目录生成 100 张图片后记录帧时间和 GDI 对象数。
- 单击历史项改为在鼠标抬起后触发粘贴，并增加目标输入焦点诊断。
- 粘贴路径收敛为 CopyQ 风格：恢复目标前台、等待用户修饰键释放，再分段发送并成对释放 Ctrl+V，避免干扰目标应用输入状态。
- 纯文本与 HTML 使用稳定语义哈希合并，保留更完整的 HTML 记录并提升到历史开头；自写回粘贴继续过滤。
- 加强 DIB/DIBV5 的头部、压缩类型、颜色表、stride、像素长度和整数范围校验，并补充预览共用校验逻辑。
- 检查剪贴板恢复路径的 `GlobalLock`、`SetClipboardData` 和所有权转移；存储追加、重建、重加密和设置写入失败时保留旧数据。
- 搜索任务改为单任务可取消并在窗口关闭时回收，设置文件改为临时文件写入后原子替换。
- 修复历史重建、重加密和重复记录更新后重启时列表顺序反转的问题。
- 修复 HTML 记录搜索误匹配内部格式头、HTML 属性和偏移数字的问题。
- x64 Release 构建和隔离 `ClipLiteStore` CTest 通过；存储测试包含重复文本、HTML 丰富化和重启顺序验证。

## [1.0.0] - 2026-08-23

ClipLite 首个正式版本，提供 Windows x64 安装包和便携包。

### 用户功能

- 提供剪贴板历史捕获、搜索、筛选、置顶、删除、清空和自动粘贴。
- 支持纯文本、HTML、文件列表、DIB 和 DIBV5 图片的保存与恢复。
- 支持 `Alt+V`，并提供可选的 `Win+V` 替换和快捷键自定义。
- 提供英文/简体中文界面、自动/浅色/深色主题和多种强调色。
- 提供最大记录数、磁盘空间、保留天数、单条内容大小和暂停监听设置。
- 提供来源应用过滤、敏感文本过期和 Windows DPAPI 用户级加密。
- 提供普通安装模式和便携模式，便携包包含 SHA-256 校验文件。

### 稳定性和隐私

- 使用追加式 v4 历史格式，带长度校验、CRC 校验、截断恢复和临时文件原子替换。
- 启动时只读取历史元数据和短预览，完整正文与图片按需读取。
- 搜索支持 Unicode 文本，并将大数据量搜索放到临时后台任务执行。
- 诊断日志不记录剪贴板正文和 payload，并限制日志大小。
- 明确 Windows UIPI、管理员窗口、UAC 安全桌面和其他桌面会话的限制。

### 发布文档

- 增加 `LICENSE.md`，采用 PolyForm Noncommercial License 1.0.0。
- 增加面向贡献者的 `CONTRIBUTING.md` 和安全问题处理说明 `SECURITY.md`。
- 增加中英文用户、贡献者和安全文档，以及功能截图。
- 在中英文 README 中增加本机 Release 的内存、启动耗时、GDI/USER 资源和程序体积实测基线。
- 在中英文 README 中增加面向普通用户的同类工具定位对比，突出轻量、原生、本地优先、便携和格式支持优势。
- 在中英文 README 中补充项目初衷，说明 AI 应用增长背景下节省存储和内存资源的设计目标。
- 统一 CMake、Windows 文件资源、安装器、便携打包脚本、关于页和 HTML 原型中的版本号为 `1.0.0`。

### 验证

- x64 Release 构建通过。
- `ctest --test-dir build-x64 -C Release --output-on-failure` 通过。
- 存储测试覆盖正常读写、重启恢复、Unicode 搜索、CRC 损坏、尾部截断、DPAPI 和 10000 条记录压力场景。
- 已完成历史窗口打开/关闭压力测试和文本剪贴板捕获压力测试；不同 Windows 版本、DPI、干净环境及 Defender/SmartScreen 的最终外部验证仍需发布者继续执行。
