# 更新日志

## [未发布]

### UI

- 历史窗口置顶状态持久化到设置文件，重新打开或重启程序后保持上次状态。
- 优化历史窗口四角、外框和内容边界：统一圆角几何，所有历史窗口圆角元素使用 GDI+ 抗锯齿，并限制绘制内容不越过内侧边框。
- 通用设置新增可选“以管理员权限运行”开关；启用后通过 UAC 重启并等待旧实例退出，关闭后下次启动恢复普通权限。
- 修复启用 Win+V 替换后单独按 Win 键无法打开开始菜单的问题。
- 修复单独触发 Win 键后只注入按下事件、导致后续 G 键被系统识别为 Win+G 而无法输入的问题；现在按下和抬起事件成对恢复。
- 修复 Win+V 历史窗口置顶后点击外部仍被鼠标钩子关闭的问题，并忽略置顶切换过程中已排队的关闭消息。
- 修复 Win+V 偶发丢失 Win 键抬起事件后普通按键被吞掉的问题；现在会根据真实 Win 键状态即时恢复，避免 G 等按键失效。
- Win+V 低级钩子安装失败时增加 `RegisterHotKey` 兜底，避免快捷键设置已启用但完全无响应。
- 按照 Windows 低级钩子时序重做 Win+V 释放确认，改由消息循环定时器读取 Win/V 状态，避免在钩子回调内读取未更新的异步键状态。
- 修复设置窗口失焦或快捷键配置触发钩子重挂后 Win + V 偶发失效的问题；Win 键转发现在成对释放，避免 Win 键状态残留、开始菜单误弹和普通 Win 快捷键被吞掉。
- 修复设置窗口关闭或失焦时后台唤起历史弹窗被初始失活消息立即关闭的问题；当前台激活被 Windows 拒绝时仍保持弹窗可见。
- 修复历史弹窗打开阶段误判激活状态导致的无操作自动关闭；普通入口仍保留真正失焦后的自动关闭行为。
- 失焦关闭改为短延迟复核当前前台窗口，避免 Windows 瞬时激活消息误触发关闭。
- Win + V 改为在完整组合键释放后再打开历史窗口，避免按键释放过程触发前台切换后自动关闭。
- 修复后台弹窗未真正成为前台却被激活消息误标记，导致约 1 秒后自动关闭的问题。
- 修复 Win+V 打开后 Windows 恢复原前台窗口时的误关闭；Win+V 弹窗改由外部鼠标点击、Esc、关闭入口或粘贴操作结束生命周期。
- 修复强制替换 Win + V 时开始菜单被错误唤起、Win 键状态残留和 Alt + V 失效的问题；同时将单实例检查提前到历史存储打开之前，避免重复启动引发启动失败弹窗刷屏。
- 发布方式拆分为普通安装模式和便携模式：安装模式支持自定义安装目录和 Windows 标准卸载入口，便携模式将数据保存在程序目录下的 `data` 文件夹。
- 通用设置新增“启动后打开设置”和“启动后显示系统提示”开关；启动提示使用 Windows 系统托盘通知，旧配置未写入新字段时保持启动后打开设置的原有行为。
- 启动存储、后台窗口、单实例锁或剪贴板监听初始化失败时显示固定错误提示，避免程序静默退出。
- 增加不包含剪贴板正文和 payload 的诊断日志，记录启动、初始化、存储、快捷键和粘贴失败；日志限制为 1 MB，并在“关于 ClipLite”中提供打开入口。
- 设置页完成英文和简体中文文案同步；设置页文案集中在语言包中，语言切换会立即刷新窗口标题、按钮、布局和所有统计/提示文本。
- 历史窗口、筛选菜单、右键菜单、托盘菜单和预览占位符纳入同一语言包；修复历史窗口打开期间新增剪贴板不刷新列表的问题。
- 修复橙色强调色区域未绘制但仍可点击的问题；移除历史右键菜单中重复的置顶入口，并补充粘贴目标失效时的失败反馈。
- 降低历史保留限制且可能删除现有记录时增加确认弹窗；取消操作会恢复原限制，避免自动同步直接丢失历史。
- 设置页按功能重新分组：通用页仅保留界面与系统行为，快捷键页拆分全局/系统快捷键与注册状态，存储页集中保留策略、分类限制和历史统计，隐私页增加独立的保护范围板块。
- 存储管理页增加自定义缓存目录，切换目录时安全迁移现有历史并自动保存路径设置。
- 设置页和历史窗口将纯文本、富文本统一为文本；存储分类限制和清理改为文本、图片、文件三类。
- 历史项目支持右键选择自动粘贴、粘贴为纯文本或粘贴为富文本；增加 `Ctrl+Shift+V` 和 `Ctrl+Shift+R` 快捷键。
- 快捷键设置页统一管理打开历史、打开设置、暂停监听，以及历史窗口内的粘贴、格式粘贴、关闭、筛选、删除和打开设置快捷键。
- 将“强制替换 Win + V”移到快捷键页顶部的重点系统快捷键区域；历史窗口为键盘当前选中项增加明显的强调背景、边框和侧边标记。
- 图片缩略图改为按比例适配可视区域，停止滚动后使用 GDI+ 高质量插值；滚动期间使用轻量缩放降低 CPU 占用。
- 富文本历史预览改为显示剪贴板中的纯文本内容，无法提取时才回退为 HTML 占位文本。
- 设置页开关背景与滑轨统一使用胶囊圆弧绘制，输入框边框改为内侧物理 1px，并保留主题强调色焦点阴影。
- 修复设置页滚动闪烁：控件位置使用批量提交，滚动时不再重复配置 EDIT 文本区域，非多行编辑控件会将滚轮消息转发到设置页。
- 修复输入时 EDIT 背景擦除造成的闪烁，输入框背景、原生文字、占位符和边框现在统一通过双缓冲合成后一次提交。
- 设置页数值输入增加与业务范围匹配的字符长度限制；焦点框与边框统一坐标，鼠标移动到控件时稳定显示 I-beam 或手形指针。
- 数值输入框清空后失焦会恢复为当前字段允许的最小值，避免空值继续进入自动同步流程。
- 统一输入框裁剪圆角与焦点边框几何，收敛光标更新路径避免输入时闪烁，并在输入数字时去除前导零。
- 设置页不再与 Windows 的“输入时隐藏鼠标指针”策略冲突；鼠标光标仅根据实际所在控件区域更新。
- 存储管理页清理按钮改为低饱和危险操作样式，支持悬停、按下和键盘焦点反馈；清理后在标题栏显示成功、无记录或失败状态并自动恢复。
- 所有设置页历史清理入口在删除前显示包含记录数量的确认弹窗，取消操作也会给出反馈；清理后同步刷新历史窗口。
- 输入框最终边框改为 GDI 物理 1px 实线，GDI+ 仅负责清理旧边框和绘制轻量焦点阴影，避免抗锯齿描边造成视觉加粗。
- 修复输入框聚焦框与边框圆角半径不一致的问题，统一 GDI 与 GDI+ 的圆角几何参数。
- 设置页按 HTML 参考原型重排为 `180px` 左侧导航、固定标题区和四张白色分组卡片，正文支持滚动，原生输入框和开关与卡片坐标统一。
- 设置页移除保存按钮，开关、下拉框和输入框自动同步；Debug 构建增加卡片、正文视口、滚动偏移和输入框边界诊断标记，Release 不绘制诊断层。
- 设置页滚动改用双缓冲并避免无条件重排子控件，固定标题区会隐藏越界控件，同时移除设置窗口右侧和下侧的类阴影线。
- 设置页改为卡片式布局，复选项使用开关样式，下拉框统一颜色和选中态。
- 历史窗口增加可点击的自动类型筛选：全部、置顶、文本、图片、文件和其他；文本筛选包含纯文本与富文本。
- 历史窗口右键菜单增加窗口置顶；置顶后失去焦点仍保持打开，支持连续粘贴。
- 历史窗口顶部增加置顶和关闭图标；置顶模式粘贴时临时隐藏窗口，粘贴完成后恢复，避免顶层窗口拦截目标应用输入。
- 历史列表使用类型徽章和更清晰的选中态，移除手动分类编辑入口。
- 修复历史窗口高度计算、滚动时重复搜索和列表区域误点击问题，并使用双缓冲降低滚动闪烁。
- 修复历史窗口系统黑色边框覆盖圆角的问题，提升搜索框、标题、筛选项和历史条目的字号与间距。
- 历史窗口按原型图改为 320x500 轻量卡片布局，采用顶部搜索、横向筛选标签、白色内容卡片和细滚动条。
- 剪贴板历史新增来源进程记录，支持常见应用名称友好化显示；当前开发格式固定为 v4。
- 历史卡片统一高度，图片卡片只显示图片缩略图，不显示图片文件名。
- 进一步对齐原型视觉参数：顶部间距、搜索框边框、筛选栏溢出、卡片区域和窗口阴影。
- 历史窗口支持标题区域拖拽、筛选栏横向滚动，清空按钮改为清除搜索内容。
- 搜索框增加默认提示并调整单行光标垂直对齐；空历史改为居中图标和文案状态。
- 卡片元数据按原型细化为来源标签、类型标签、时间、置顶按钮和悬停删除按钮，分别匹配原型字号、颜色、边框和圆角。
- 修复拖拽筛选栏时因窗口瞬时失焦导致历史窗口关闭的问题。
- 修复筛选栏拖拽时搜索框 placeholder 因整窗背景擦除而闪烁的问题。
- 调整历史筛选顺序，将置顶筛选移动到全部筛选旁边。
- 设置窗口改为左侧导航、右侧分组卡片、主题预览和右对齐控件的现代设置页布局。
- 设置页导航改为可操作的通用、存储管理、安全与隐私、关于 ClipLite 四个页签。
- 设置页开关改为固定边界的真实轨道/滑块组件，使用系统复选状态和 Windows GDI+ 抗锯齿绘制，避免模糊、锯齿和控件外溢。
- 设置页新增与通用页同级的快捷键页，展示 `Alt+V`、`Ctrl+Alt+S`、`Ctrl+Shift+P`、`Win+V` 和 `F10` 的当前行为，并提供全局快捷键录入。
- `settings-prototype.html` 更新为五个同级页签的完整设置页 HTML 原型，包含黑白主题、自动同步状态和三组快捷键录入交互。
- 设置页视觉基线对齐 `740x520` 原型：卡片圆角调整为 10px、内容边距调整为 20px、输入框调整为 150x30，并同步 DWM 标题栏深浅主题颜色。
- 设置页增加自动/浅色/深色主题模式和蓝色、紫色、旧绿色、橙色强调色；强调色同时应用到设置页、开关、下拉框、输入焦点和历史窗口。
- 存储管理页支持按纯文本、富文本、图片和文件设置独立最大记录数、最大空间和清理操作，0 表示不限，置顶记录不会被分类自动清理。
- 设置页布局改为共享行模型：每行统一计算标签区域、控件区域、行高和上下间距，卡片高度与滚动范围由行布局结果自动累加，避免控件尺寸变化造成挤压或溢出。
- 设置页开关增加约 160ms 的滑块过渡动画，仅使用窗口定时器驱动当前开关。
- 修复开关双击和快速重复点击只触发一次的问题；动画反向时从当前插值位置继续，并修正语言下拉框快速开合的目标方向。
- 优化开关外层矩形盒子：不再绘制可见外层容器，承载区域与卡片背景融合，视觉上只保留滑轨和滑块。
- 修复主题切换时开关承载区出现矩形色差；主题动画按统一帧进度绘制，滑轨使用完整半高圆角胶囊几何。
- 存储管理页增加纯文本、富文本、图片和文件分类的独立清理按钮。
- 设置窗口增加横向空间和底部留白，长标签、数值输入框和操作按钮在常见 DPI 下不再拥挤。
- 历史窗口改为圆角卡片式布局，统一背景、选中态、分隔线、计数和底部操作提示。
- 历史窗口失去应用焦点后自动关闭。
- 设置页和历史窗口统一字体、深浅主题和输入控件配色。
- 设置页增加数据统计和更清晰的分组间距；当前分类限制按文本、图片、文件类型工作。
- 设置窗口按当前鼠标所在显示器工作区居中。
- 设置页语言下拉框改为自绘圆角 popup，支持选中态、hover 行和平滑展开/收起动画。
- 深浅主题切换增加约 180ms 的缓动调色板过渡，设置页、历史弹窗、输入控件和窗口边框同步变化；动画只使用短时窗口定时器和即时颜色插值，不保留截图缓存。
- 设置页数字和多行输入框改为原生 EDIT 内部绘制，数字项使用真正的单行样式并支持 Tab 导航；ClipLite 统一提供主题背景、圆角边框、内边距和焦点/hover 样式。
- 多行忽略应用输入框为空且未聚焦时显示本地化格式提示，聚焦后自动消失；输入框滚动到固定标题区或窗口底部时整体隐藏，避免半截控件露出。
- 输入框焦点使用主题强调色边框并保持文字垂直对齐；开关尺寸对齐原型为 36x20，支持快速连续点击时从当前位置平滑反向动画。
- 修复下拉框和开关连续点击时的输入丢失，不再因动画进行中忽略后续点击。
- 历史窗口和设置页按交互场景切换鼠标指针，并为可点击区域增加 hover 状态；hover 重绘限制在局部区域，避免界面闪烁。
- 修复强制 Win+V 模式释放 Win 键后弹出系统开始菜单的问题。
- 历史面板增加参考 Windows 剪贴板风格的顶部清除入口和卡片留白。

### 新增

- 设置页支持 1-32 MB 单条内容大小上限，超限的新剪贴板内容不会写入历史。
- 设置页支持按行配置忽略应用来源，匹配不区分大小写。
- 设置页支持可选的敏感文本过期时间，默认关闭，过期时间持久化在 v4 历史记录中。
- 程序、任务栏窗口、历史弹窗和系统托盘统一使用 ClipLite 工具图标。
- 工具图标更新为深色渐变剪贴板与白色 C 标记设计，并保留 SVG 源稿。
- 增加 10000 次文本剪贴板捕获压力脚本，测试结束后恢复原文本剪贴板。
- 图片历史项支持可见行延迟缩略图绘制，不在启动时解码全部图片。
- 历史右键菜单支持按类型和置顶状态筛选。
- 增加 Windows 文件版本资源和统一版本信息。
- 增加 `tools/measure.ps1`，用于测量工作集、私有内存和程序体积。
- 数据保留设置现在会实际执行最大记录数、磁盘空间和过期清理，并保留置顶项。
- 设置页支持随 Windows 启动；Explorer 重启后自动恢复托盘图标。
- 设置页增加文本、HTML、图片、文件和置顶统计。
- 进程启用 Per-Monitor DPI 感知，搜索框支持 Tab/方向键切换到历史列表。
- 存储测试增加空内容、超限内容和 10000 条记录压力覆盖。
- 剪贴板监听注册失败时显示错误并安全退出。
- 增加便携发布脚本和 SHA-256 校验文件生成。
- 发布构建改为显式 x64，便携包使用 `build-x64` 输出。
- 设置页支持可选 Windows DPAPI 用户级历史加密，并支持重新加密迁移。
- 搜索结果限制为最多 5000 条，并增加中文 Unicode 搜索回归测试。
- 增加 `--history`、`--settings` 和 `--exit` 启动参数。
- 捕获 DIB/DIBV5 前增加图片头部和尺寸有效性校验。
- README 增加数据目录、清理、DPAPI 和 Windows 权限限制说明。
- 增加历史窗口生命周期压力脚本，已通过 1000 次打开/关闭测试。
- 历史记录格式保留分类字段；当前界面按剪贴板类型自动分类，不提供手动分类名称编辑。
- 当前 x64 Release 便携包已重新生成，程序体积约 240.5 KB。
- 历史窗口支持鼠标滚轮和键盘选择自动滚动。
- 设置页支持最大记录数、最大磁盘空间、保留天数和暂停监听。
- 粘贴历史失败时提供系统提示音反馈。
- 支持 `CF_HTML`，恢复时同时提供富文本和纯文本格式。
- 支持 `CF_DIBV5` 图片格式，并按原格式恢复。
- 剪贴板被其他程序占用时进行有限重试。
- 历史文件 v2 格式增加 CRC 校验。
- 启动时检测尾部截断、长度异常和校验失败记录。
- 旧版 v1 历史记录读取后自动安全重建为 v2。
- 双击 `ClipLite.exe` 时打开设置窗口。
- 已运行实例收到再次启动请求时打开设置窗口。
- 语言设置增加“自动”选项。
- 默认语言跟随 Windows 首选 UI 语言，当前支持英文和简体中文判断。

### 修改

- `Alt+V` 和托盘“打开历史”继续打开剪贴板历史窗口。
- 设置页语言下拉框改为“自动 / English / 简体中文”。
- 托盘入口和双击入口职责分离，避免启动后直接显示历史窗口。

### 验证

- 使用 `740x520` 设置窗口完成 Release 实际截图，对比确认侧栏、标题区、卡片间距、控件右对齐和正文滚动；HTML 原型在移动宽度下固定 `740px`，记录为窄视口溢出参考而非移动端运行目标。
- Release 构建、Debug 构建和 `ClipLiteStore` CTest 均通过。
- 主题过渡改动完成 Release/Debug 构建，`ClipLiteStore` CTest 通过，窗口生命周期压力测试 `100/100` 通过；资源测量为 Working Set `16.62 MB`、Private Bytes `2.85 MB`、GDI `30`。
- Release 构建通过。
- `ClipLiteStore` CTest 通过。
- 存储测试覆盖自定义 payload 大小上限，超限内容会被拒绝。
- 存储测试覆盖 v4 过期记录、最大记录数、临时文件残留和类型统计。
- 双击启动后 `ClipLiteSettings` 窗口可见性已实测。
- 临时使用 `language=-1` 验证新安装场景，语言下拉框默认选中“自动”。
- 存储测试覆盖历史顺序、完整搜索、CRC 校验和尾部截断恢复。
- 存储测试覆盖 HTML 和 DIBV5 类型的持久化恢复。
