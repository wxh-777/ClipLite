# 更新日志

## [1.0.5] - 2026-08-26

修复历史窗口搜索输入焦点问题，并增加中文输入法兼容模式设置。

### 修复与体验

- 普通搜索默认点击后直接获得输入焦点，避免搜索文字进入原应用。
- 修复搜索输入被误判为外部操作而关闭历史窗口的问题。
- 兼容模式下保留原应用前台，使用 `Ctrl+Space` 临时进入真实输入法焦点，且切换快捷键的空格不会写入搜索框。
- 设置页新增“搜索输入兼容模式”开关，默认关闭并自动保存。

### 构建与验证

- 统一程序、资源、CMake、安装器、打包脚本和中英文 README 版本为 `1.0.5`。
- x64 Release 构建、`ClipLiteStore` CTest 和历史窗口压力测试 `100/100` 通过。

## [1.0.4] - 2026-08-25

精简安装包和便携包中的文档资源，界面截图改由 GitHub 按版本托管。

### 发布调整

- 中英文 README 的界面截图改为固定的 `v1.0.4` GitHub Raw 地址。
- 安装包和便携包不再复制 `docs/screenshots`，保留仓库中的原图作为线上来源。
- 打包脚本增加当前用户目录下 Inno Setup 的自动检测。
- 统一程序、Windows 文件资源、构建配置、安装器、打包脚本和中英文 README 版本为 `1.0.4`。

## [1.0.3] - 2026-08-25

完善设置窗口、历史加密提示和支持作者功能，并精简发布包中的本地资源。

### 功能与体验

- 设置窗口增加最小化能力，支持再次点击任务栏图标最小化，同时保持窗口不可缩放和不可最大化。
- Windows 用户加密开关增加 800 毫秒防抖，连续切换只迁移一次最终状态，关闭设置窗口时立即提交。
- 隐私设置增加迁移、跨用户/设备解密限制及大型图片可能影响剪贴板和列表滚动性能的中英文提醒。
- 支持作者和 QQ 群窗口改由独立辅助进程运行，关闭设置窗口或主程序时同步回收。
- 支持图片改为联网异步加载，移除安装包和便携包中的本地支付及 QQ 群图片资源。
- 历史列表滚轮输入改为直接更新滚动位置，移除不再使用的滚动补间定时器。

### 构建与验证

- 统一 CMake、Windows 文件资源、安装器、打包脚本、关于页和中英文 README 版本为 `1.0.3`。
- 更新中英文界面截图，并增强 MSVC Release 的函数级链接优化和无增量链接配置。
- x64 Debug、Release 构建和隔离 `ClipLiteStore` CTest 通过。

## [1.0.2] - 2026-08-24

修复历史窗口夺取输入焦点的问题，使粘贴交互接近 Windows 系统剪贴板行为。

修复 UTF-8 文本预览在字符边界截断时显示为无效文本的问题；完整文本和粘贴内容不受影响。

### 修复与验证

- 历史窗口改为非激活窗口，原应用输入框保留系统焦点和光标状态。
- 通过临时键盘钩子将上下、翻页、回车和 Esc 等列表控制键交给历史窗口处理。
- 用户直接输入普通字符时关闭历史窗口，并让字符继续进入原应用输入框。
- 移除 UI Automation 文本改写和不再使用的 `uiautomationcore` 链接，保留标准 Ctrl+V 粘贴路径。
- x64 Release 构建和隔离 `ClipLiteStore` CTest 通过。

## [1.0.1] - 2026-08-24

修复一次剪贴板变更被分阶段通知时产生重复文本记录的问题，并强化异常剪贴板数据、存储失败路径和搜索线程的生命周期处理。

### 修复与验证

- 历史窗口缓存最近显示的图片缩略图，滚动时避免重复读取和解码原图；大图 CRC 校验改为查表实现。
- 新增自包含图片滚动基准，使用进程专属临时目录生成 100 张图片后记录帧时间和 GDI 对象数。
- 单击历史项改为在鼠标抬起后触发粘贴，并增加目标输入焦点诊断。
- 粘贴路径收敛为 CopyQ 风格：恢复目标前台、等待用户修饰键释放，再分段发送并成对释放 Ctrl+V，避免干扰目标应用输入状态。
- 纯文本与 HTML 使用稳定语义哈希合并，保留更完整的 HTML 记录并提升到历史开头；自写回粘贴继续过滤。
- 加强 DIB/DIBV5 的头部、压缩类型、颜色表、stride、像素长度和整数范围校验，并补充预览共用校验逻辑。
- 检查剪贴板恢复路径的 `GlobalLock`、`SetClipboardData` 和所有权转移；存储追加、重建、重加密和设置写入失败时保留旧数据。
- 搜索任务改为单任务可取消并在窗口关闭时回收，设置文件改为临时文件写入后原子替换。
- 修复历史重建、重加密和重复记录更新后重启时列表顺序反转的问题。
- 修复 HTML 记录搜索误匹配内部格式头、HTML 属性和偏移数字的问题。
- x64 Release 构建和隔离 `ClipLiteStore` CTest 通过；存储测试包含重复文本、HTML 丰富化和重启顺序验证。

## [1.0.0] - 2026-08-23

ClipLite 首个正式版本，提供 Windows x64 安装包和便携包。

### 用户功能

- 提供剪贴板历史捕获、搜索、筛选、置顶、删除、清空和自动粘贴。
- 支持纯文本、HTML、文件列表、DIB 和 DIBV5 图片的保存与恢复。
- 支持 `Alt+V`，并提供可选的 `Win+V` 替换和快捷键自定义。
- 提供英文/简体中文界面、自动/浅色/深色主题和多种强调色。
- 提供最大记录数、磁盘空间、保留天数、单条内容大小和暂停监听设置。
- 提供来源应用过滤、敏感文本过期和 Windows DPAPI 用户级加密。
- 提供普通安装模式和便携模式，便携包包含 SHA-256 校验文件。

### 稳定性和隐私

- 使用追加式 v4 历史格式，带长度校验、CRC 校验、截断恢复和临时文件原子替换。
- 启动时只读取历史元数据和短预览，完整正文与图片按需读取。
- 搜索支持 Unicode 文本，并将大数据量搜索放到临时后台任务执行。
- 诊断日志不记录剪贴板正文和 payload，并限制日志大小。
- 明确 Windows UIPI、管理员窗口、UAC 安全桌面和其他桌面会话的限制。

### 发布文档

- 增加 `LICENSE.md`，采用 PolyForm Noncommercial License 1.0.0。
- 增加面向贡献者的 `CONTRIBUTING.md` 和安全问题处理说明 `SECURITY.md`。
- 增加中英文用户、贡献者和安全文档，以及功能截图。
- 在中英文 README 中增加本机 Release 的内存、启动耗时、GDI/USER 资源和程序体积实测基线。
- 在中英文 README 中增加面向普通用户的同类工具定位对比，突出轻量、原生、本地优先、便携和格式支持优势。
- 在中英文 README 中补充项目初衷，说明 AI 应用增长背景下节省存储和内存资源的设计目标。
- 统一 CMake、Windows 文件资源、安装器、便携打包脚本、关于页和 HTML 原型中的版本号为 `1.0.0`。

### 验证

- x64 Release 构建通过。
- `ctest --test-dir build-x64 -C Release --output-on-failure` 通过。
- 存储测试覆盖正常读写、重启恢复、Unicode 搜索、CRC 损坏、尾部截断、DPAPI 和 10000 条记录压力场景。
- 已完成历史窗口打开/关闭压力测试和文本剪贴板捕获压力测试；不同 Windows 版本、DPI、干净环境及 Defender/SmartScreen 的最终外部验证仍需发布者继续执行。
