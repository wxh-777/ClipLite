# 更新日志

## [未发布]

### 新增

- 双击 `ClipLite.exe` 时打开设置窗口。
- 已运行实例收到再次启动请求时打开设置窗口。
- 语言设置增加“自动”选项。
- 默认语言跟随 Windows 首选 UI 语言，当前支持英文和简体中文判断。

### 修改

- `Alt+V` 和托盘“打开历史”继续打开剪贴板历史窗口。
- 设置页语言下拉框改为“自动 / English / 简体中文”。
- 托盘入口和双击入口职责分离，避免启动后直接显示历史窗口。

### 验证

- Release 构建通过。
- `ClipLiteStore` CTest 通过。
- 双击启动后 `ClipLiteSettings` 窗口可见性已实测。
- 临时使用 `language=-1` 验证新安装场景，语言下拉框默认选中“自动”。
