# ClipLite 执行计划

## 目标

ClipLite 是一个仅支持 Windows 的原生剪贴板历史工具。

开发优先级：

1. 常驻内存和使用时内存最低
2. 数据可靠性和剪贴板兼容性
3. 操作流畅度
4. Windows 风格的交互和视觉表现
5. 程序体积和发布便利性

技术边界：C++17、CMake、Win32、GDI、系统 DLL。禁止引入 Electron、WebView、WinUI、XAML、Qt、Flutter、React Native、SQLite 和常驻 GPU 渲染资源。

## 状态约定

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成并通过该阶段验证
- 每完成一个阶段，必须先更新本文件，再进入下一阶段。
- 每个阶段的验证命令、测试结果、已知问题和资源测量必须写入对应的“完成记录”。

## 当前基线

- `[x]` 已创建独立项目目录：`F:\jobFile\My\ai-job\github-ject\ClipLite`
- `[x]` 已建立 CMake、Win32 主程序、存储模块和存储测试
- `[x]` 已实现 `Alt+V`
- `[x]` 已实现可选强制替换 `Win+V`
- `[x]` 已实现鼠标附近弹出历史窗口
- `[x]` 已实现单击历史项目粘贴到原活动窗口
- `[x]` 已实现文本、文件列表、DIB 图片的基础捕获和恢复
- `[x]` 已实现追加式历史存储、搜索、置顶、删除和分类
- `[x]` 已实现英文/简体中文和黑白主题设置，设置页文案集中语言包管理
- `[x]` 已实现双击打开设置窗口和系统托盘入口
- `[x]` Release 主程序约 `211.5 KB`
- `[x]` 已测得后台 Private Bytes 约 `1.67 MB`
- `[x]` 已测得后台 Working Set 约 `9.81 MB`

## 阶段一：工程和资源基线

目标：固定构建方式、目录结构、诊断方式和内存测量方法。

- `[x]` 确认 Release 构建使用 Visual C++ 和静态 CRT
- `[x]` 确认 CMake 路径并加入当前用户 PATH
- `[x]` 固定 x64 Release 构建配置（`-A x64`）
- `[x]` 增加 Debug、Release、测试构建说明
- `[x]` 增加统一的版本号和 Windows 文件版本资源
- `[x]` 增加资源测量脚本，记录 Working Set、Private Bytes、启动时间和文件体积
- `[x]` 增加 `.gitignore`，排除 `build/`、用户数据和测试生成物
- `[x]` 确认项目根目录不提交编译产物

完成记录：

- 构建命令：`F:\jobSoftware\cmake\PFiles64\CMake\bin\cmake.exe --build build --config Release`
- 当前结果：Release 构建通过，存储测试通过。
- 本次新增：Windows 文件版本资源和 `tools/measure.ps1` 资源测量脚本。
- 本次验证：`tools/measure.ps1` 可从项目根目录运行，并输出进程启动时间、Working Set、Private Bytes 和文件体积。
- x64 验证：使用 `-A x64` 构建通过，CTest 通过。
- 待完成：干净 Windows 环境验证。

## 阶段二：后台生命周期和系统集成

目标：后台监听稳定、低开销、可发现、可退出、可单实例唤醒。

- `[x]` 创建 message-only window
- `[x]` 使用 `AddClipboardFormatListener` 监听剪贴板变化
- `[x]` 实现单实例互唤醒
- `[x]` 实现系统托盘图标
- `[x]` 实现双击启动后按设置选择是否显示设置窗口
- `[x]` 增加启动系统托盘提示开关
- `[x]` 增加启动失败固定错误提示
- `[x]` 增加不记录剪贴板正文的诊断日志和打开入口
- `[x]` 增加普通安装模式和便携模式发布结构
- `[x]` 实现关闭历史窗口后继续后台运行
- `[x]` 实现托盘打开、设置和退出
- `[x]` 增加 Windows 登录启动选项
- `[x]` 增加托盘图标重建处理，适配 Explorer 重启
- `[x]` 增加监听器注册失败和系统恢复逻辑
- `[x]` 增加退出时所有窗口、钩子、托盘资源的统一清理
- `[x]` 验证后台空闲不轮询、不创建常驻工作线程

完成记录：

- 当前结果：message-only window、剪贴板监听、单实例、托盘和启动行为设置已完成。
- 本次验证：启动行为可分别控制设置窗口和系统托盘提示；`--history`、`--settings` 参数仍可强制打开对应窗口。
- 本次完成：存储、后台窗口、单实例锁或剪贴板监听初始化失败时显示固定错误提示后退出。
- 本次完成：诊断日志写入 `%LOCALAPPDATA%\\ClipLite\\cliplite.log`，限制 1 MB，并在关于页提供打开入口；日志不包含剪贴板正文和 payload。
- 本次完成：便携包使用程序目录下的 `portable.flag` 和 `data\\`，普通安装器脚本使用用户可选安装目录并保留用户数据目录独立性。
- 本次验证：剪贴板监听注册失败会显示错误并退出；后台采用消息通知，无轮询和常驻工作线程。
- 待完成：生命周期压力测试。

## 阶段三：剪贴板格式兼容

目标：可靠支持常用剪贴板格式，防止格式丢失、死锁和异常数据导致崩溃。

- `[x]` 支持 `CF_UNICODETEXT`
- `[x]` 支持 `CF_HDROP`
- `[x]` 支持 `CF_DIB`
- `[x]` 完善 `CF_DIBV5` 支持
- `[x]` 增加 `CF_HTML` 捕获和恢复
- `[x]` 设计文本、HTML、图片、文件列表的统一记录格式
- `[x]` 增加剪贴板被其他进程占用时的有限重试
- `[x]` 增加空内容、超大内容和损坏数据测试
- `[ ]` 增加损坏句柄和不可锁定句柄测试
- `[x]` 增加自写回过滤，避免恢复历史时产生重复记录
- `[x]` 增加来源窗口和来源进程记录策略
- `[x]` 明确未知自定义格式的默认忽略策略
- `[x]` 明确密码、Token 和敏感内容的处理策略

完成记录：

- 当前结果：文本、文件列表、DIB、DIBV5 和 HTML 具备捕获与恢复；HTML 同时保留富文本和纯文本回退。
- 本次验证：HTML、DIBV5 类型可持久化重启恢复，剪贴板打开失败会进行有限重试，未知格式默认忽略。
- 待完成：异常句柄测试和敏感内容隐私策略。

## 阶段四：持久化存储和崩溃恢复

目标：低内存、低体积、可恢复的本地历史存储。

- `[x]` 使用追加式二进制文件保存记录
- `[x]` 内存中只保存记录元数据、预览、文件偏移和长度
- `[x]` 启动时只读取元数据和短预览
- `[x]` 支持单条内容按需读取
- `[x]` 支持哈希去重
- `[x]` 支持删除、置顶和分类持久化
- `[x]` 支持文件重建
- `[x]` 增加文件头、格式版本和完整性校验
- `[x]` 增加 CRC 或校验和
- `[x]` 增加截断记录检测和启动时安全恢复
- `[x]` 使用临时文件加写入后替换，确保整理过程中不损坏旧数据
- `[x]` 增加磁盘空间不足处理
- `[x]` 增加最大历史条数配置
- `[x]` 增加最大磁盘空间配置
- `[x]` 增加自动过期配置
- `[ ]` 增加后台低优先级整理，而不是在用户操作中长时间阻塞
- `[x]` 评估并实现 DPAPI 用户级加密（默认关闭）
- `[x]` 增加 1、1000、10000 条记录的存储压力测试

完成记录：

- 当前结果：追加式存储、索引、去重、按需读取和基础重建已完成。
- 本次验证：当前 v4 记录可重启读取；CRC 校验可拒绝损坏记录；尾部截断后可保留前面的有效记录。
- 本次验证：设置页可配置最大记录数、最大磁盘空间和保留天数，启动与保存设置时执行清理并保留置顶项。
- 本次验证：DPAPI 加密历史可在同一用户下重启读取，并可重新解密迁移。
- 本次完成：追加路径同步应用最大记录数，清理、删除、分类和重建失败时恢复内存索引；启动时清理残留临时文件。
- 本次完成：v4 记录支持可选过期时间，设置默认关闭，后台定时清理已过期敏感记录。
- 本次验证：空内容和超过 32 MB 的内容会被拒绝；10000 条记录追加、重启读取和清理测试耗时约 6.43 秒。
- 待完成：后台低优先级整理、磁盘错误和长期运行压力测试。

## 阶段五：搜索、索引和分类

目标：在不建立大型常驻索引的前提下提供可接受的搜索速度。

- `[x]` 支持摘要搜索
- `[x]` 摘要未命中时按记录读取完整内容搜索
- `[x]` 支持英文大小写不敏感搜索
- `[x]` 支持中文连续字符搜索
- `[ ]` 支持未分类、工作、代码、链接等手动分类
- `[x]` 支持置顶筛选的基础数据字段
- `[ ]` 增加分类管理界面
- `[ ]` 增加分类重命名和删除
- `[x]` 增加按类型和置顶筛选；分类筛选待实现
- `[ ]` 搜索改为临时线程执行，避免阻塞窗口消息循环
- `[x]` 对搜索缓冲区设置固定上限
- `[x]` 增加搜索结果数量上限和分页/虚拟列表
- `[ ]` 评估长历史数据下的可选小型倒排索引
- `[x]` 增加 Unicode 大小写和特殊字符测试

完成记录：

- 当前结果：基础搜索、完整文本回读搜索和分类字段已完成。
- 本次验证：历史筛选按全部、置顶、文本、图片、文件和其他映射，文本同时包含纯文本与富文本。
- 当前限制：存储格式保留 category 字段，但当前 UI 不提供手动分类编辑、重命名或分类筛选；历史窗口按文本、图片、文件和置顶筛选。
- 待完成：异步搜索和大数据量 UI 性能测试。

## 阶段六：历史窗口和粘贴交互

目标：窗口打开快、跟随鼠标、键鼠操作稳定，单击粘贴不破坏活动应用状态。

- `[x]` 历史窗口出现在鼠标附近
- `[x]` 自动避开屏幕边缘
- `[x]` 列表使用自绘方式，不为每条记录创建控件
- `[x]` 单击历史项目后恢复活动窗口并发送粘贴操作
- `[x]` 历史窗口失去应用焦点后自动关闭
- `[x]` 支持 Enter 粘贴
- `[x]` 支持 Delete 删除
- `[x]` 支持 Esc 关闭
- `[x]` 支持右键粘贴、置顶和删除
- `[ ]` 增加窗口 DPI 感知和多显示器缩放测试
- `[x]` 增加键盘焦点、Tab、上下方向键和搜索输入的完整状态机
- `[x]` 增加滚动和可见行虚拟化
- `[x]` 增加历史项图片缩略图的延迟加载
- `[x]` 统一历史窗口和设置窗口的场景指针及可点击项 hover 反馈
- `[x]` 设置页输入框统一失焦、焦点、hover、内边距、垂直对齐和深色主题样式
- `[ ]` 改进活动窗口保存逻辑，处理 UAC、桌面切换和窗口失效
- `[ ]` 改进粘贴注入策略，验证不同权限级别应用
- `[x]` 增加粘贴失败通知
- `[x]` 增加窗口打开、关闭压力测试；连续粘贴仍需真实目标应用验证

完成记录：

- 当前结果：鼠标定位、单击粘贴、键盘粘贴和基础右键操作已完成。
- 本次验证：历史列表支持鼠标滚轮和键盘选择跟随，读取或写回失败会发出系统提示音。
- 本次验证：图片仅在可见列表行绘制时从磁盘读取并由 GDI 缩放，不建立常驻图片缓存。
- 本次完成：历史窗口和设置窗口按交互场景切换鼠标指针；筛选、清空、历史卡片、置顶、删除、设置页签和设置控件均提供 hover 反馈。
- 本次完成：数字和多行输入框内部使用原生 EDIT 绘制，数字项改为单行样式并支持 Tab 导航；统一主题背景、圆角边框、内边距、单行垂直居中、焦点/失焦状态和深色背景，多行空状态显示格式提示。
- 待完成：DPI 和权限差异测试。

## 阶段七：Win+V 和快捷键策略

目标：默认稳定使用 `Alt+V`，用户启用后尽可能替换 `Win+V`，并明确失败原因。

- `[x]` 默认注册 `Alt+V`
- `[x]` 提供强制替换 `Win+V` 设置
- `[x]` 优先尝试 `RegisterHotKey`
- `[x]` 注册失败时尝试低级键盘钩子
- `[x]` 注册失败时显示提示并回退到 `Alt+V`
- `[ ]` 验证 Win 键菜单、Win+其他快捷键不被误拦截
- `[ ]` 验证管理员窗口、UAC 窗口、远程桌面和安全桌面的限制
- `[x]` 增加 Win+V 冲突检测状态显示
- `[x]` 增加快捷键自定义配置
- `[ ]` 增加卸载钩子和异常退出恢复测试
- `[ ]` 明确高权限进程和受保护窗口无法自动粘贴的限制

完成记录：

- 当前结果：Alt+V、RegisterHotKey 和低级钩子回退已实现。
- 本次完成：设置页新增与通用页同级的快捷键页，支持自定义历史、设置和暂停监听三组全局快捷键；`Win+V` 保持特殊替换开关，`F10` 保持窗口内快捷键；`settings-prototype.html` 已同步为五页原型。
- 本次完成：设置页按 `740x520` 原型收敛卡片圆角、内容边距、输入框尺寸和蓝色主题，并增加自动/浅色/深色模式、四种强调色和 DWM 标题栏颜色同步。
- 本次完成：存储页增加四类独立数量/空间限制、独立清理和滚动视图；存储层增加分类裁剪并覆盖置顶保留和测试验证。
- 本次完成：设置页通用、快捷键、存储和隐私页面改用共享行布局；行高读取实际子控件尺寸并结合标签测量自动计算，卡片、控件位置和滚动范围共享同一布局结果。
- 本次完成：设置页按功能重新分组，通用页移除存储和加密内容，快捷键页拆分全局/系统快捷键与注册状态，存储页增加独立历史统计卡片，隐私页增加保护范围卡片。
- 本次验证：开关胶囊背景、输入框物理 1px 边框、Release/Debug 构建和 `ClipLiteStore` CTest 均通过，实际截图确认各板块无控件越界。
- 本次修复：滚动控件位置改为批量更新并统一重绘，避免逐个子窗口产生闪烁；非多行输入框上的滚轮可正常滚动设置正文。
- 待完成：键盘钩子边界、安全桌面和冲突状态测试。

## 阶段八：图片和富文本低内存处理

目标：图片功能可用，但不让图片解码和缩略图拖高常驻内存。

- `[x]` 图片原始数据按磁盘记录保存
- `[x]` 图片记录启动时不解码
- `[x]` 增加 DIB/DIBV5 有效性校验
- `[x]` 增加固定尺寸缩略图生成
- `[x]` 缩略图缓存设置 8～16 MB 硬上限（当前策略为不建立常驻缓存）
- `[x]` 只解码当前可见项目
- `[x]` 历史窗口关闭时释放所有图片对象
- `[x]` 原图通过历史分类清理；缩略图不落盘、不建立独立缓存
- `[x]` 增加单张图片最大尺寸和最大字节数限制
- `[ ]` 增加超大图片、透明图片、多帧和损坏图片测试
- `[x]` 增加 HTML 预览策略，默认不引入浏览器引擎
- `[x]` 明确富文本粘贴时的格式保留和纯文本回退规则

完成记录：

- 当前结果：图片原始 DIB/DIBV5 可保存和恢复，历史窗口支持可见项延迟缩略图绘制。
- 本次验证：富文本预览显示可读纯文本，格式恢复支持自动、纯文本和富文本回退；图片停止滚动后使用 GDI+ 高质量插值。
- 待完成：超大图片、透明图片、多帧和损坏图片专项测试。

## 阶段九：设置和数据管理

目标：提供参考界面中的设置和数据管理功能，同时保持按需加载。

- `[x]` 已有黑白主题设置
- `[x]` 已有语言选择基础设置和自动跟随系统语言
- `[x]` 已有 Win+V 设置
- `[x]` 已有历史清空操作
- `[x]` 已有记录数和磁盘大小基础显示
- `[x]` 已有最大记录数、最大磁盘空间和保留天数设置
- `[x]` 增加设置窗口标签页：通用、存储管理、安全与隐私、关于 ClipLite
- `[x]` 增加数据管理标签页：存储概览、数据安全、保留设置和无常驻缓存策略
- `[x]` 增加总记录、文本、图片、文件和置顶统计
- `[x]` 增加历史文件和临时文件状态统计；缩略图/原图无独立缓存时明确为零
- `[x]` 增加独立清理按钮和清理确认
- `[x]` 清理按钮增加危险操作视觉状态、删除数量确认和成功/失败/取消反馈
- `[x]` 增加保留天数和永久保留选项
- `[x]` 增加最大内容大小（1-32 MB，限制新捕获内容）
- `[x]` 图片预览不建立常驻缓存，避免额外缓存阈值和长期内存占用
- `[x]` 增加暂停监听
- `[x]` 增加忽略应用列表（每行一个来源名称，大小写不敏感）
- `[x]` 增加可选敏感文本自动过期策略，默认关闭
- `[x]` 设置页关闭后释放临时控件和资源

完成记录：

- 当前结果：设置窗口具备最小可用版本，语言默认项为“自动”，并可配置数据保留与暂停监听。
- 本次完成：设置页改为可点击的通用、存储管理、安全与隐私、关于 ClipLite 四个页签；设置修改自动同步，保留历史清空、语言、保留策略和 DPAPI 操作。
- 本次完成：存储管理页清理按钮统一使用危险操作样式；所有清理入口在执行前确认记录数量，执行后反馈结果并刷新历史窗口。
- 本次完成：设置页滚动使用整窗双缓冲，避免滚动时界面闪烁；固定标题区以上的原生控件自动隐藏，并移除设置窗口类阴影造成的右侧、下侧多余线条。
- 本次完成：降低最大记录数、磁盘空间、保留天数或类型限额可能触发历史删除前，先显示确认弹窗；取消后恢复原输入值。
- 本次完成：按 HTML 参考原型重排设置页为 `740x520` 桌面布局，固定 `180px` 侧栏和标题区，通用页四张卡片正文支持滚动，原生控件跟随卡片坐标重排。
- 本次完成：Debug 构建增加正文视口、卡片、滚动偏移和输入框边界诊断；Release 构建不包含诊断绘制。HTML 的 `740px` 固定宽度在移动窄视口下会溢出，已作为参考原型布局问题记录。
- 本次完成：剪贴板来源进程以当前 v4 元数据持久化。
- 本次完成：存储层和剪贴板监听支持 1-32 MB 单条内容上限；设置支持按行维护忽略应用来源。
- 本次验证：自定义存储 payload 上限会拒绝超限数据，Release 构建和 `ClipLiteStore` CTest 通过。
- 本次验证：Release 实际窗口截图确认 `740x520` 桌面布局；滚轮滚动后标题和侧栏保持固定，保留策略与历史统计卡片进入可视区域。
- 本次验证：Release 和 Debug 构建均通过，`ctest --test-dir build-x64 -C Release --output-on-failure` 通过。
- 本次验证：主题过渡改动的 Release/Debug 构建和 CTest 通过，窗口生命周期压力测试 `100/100` 通过；资源测量为 Working Set `16.62 MB`、Private Bytes `2.85 MB`、GDI `30`。
- 本次验证：新安装场景 `language=-1` 时语言下拉框默认选中“自动”；已有 `language=1` 的用户偏好保持不变。
- 本次完成：存储管理页支持自定义缓存目录，切换时迁移历史文件并在目标目录成功打开后删除旧文件；默认目录仍为 `%LOCALAPPDATA%\\ClipLite`。
- 本次完成：设置页和存储管理将纯文本、富文本统一为文本，分类限额和清理改为文本、图片、文件三类；旧设置限额在加载时合并。
- 本次完成：历史项目支持右键选择自动、纯文本或富文本粘贴，并增加 `Ctrl+Shift+V`、`Ctrl+Shift+R` 快捷键。
- 本次完成：快捷键设置页统一管理全局快捷键和历史窗口内的粘贴、格式粘贴、关闭、筛选、删除及打开设置快捷键，快捷键页支持滚动。
- 本次完成：将“强制替换 Win + V”移到快捷键页顶部；历史窗口键盘选择项增加背景、边框和侧边标记，使用快捷键时可明确识别当前记录。
- 本次验证：Release 构建、`ClipLiteStore` CTest、`git diff --check` 和隔离数据窗口压力测试 `20/20` 均通过。
- 待完成：磁盘专项统计和外部环境下的长期验证。

## 阶段十：多语言和主题完善

目标：界面语言可扩展，黑白主题在所有页面和状态下保持一致。

- `[x]` 已支持英文和简体中文基础文本
- `[x]` 已支持黑白主题基础绘制
- `[x]` 历史窗口和设置控件完成第一轮统一视觉升级
- `[x]` 设置窗口按当前显示器工作区居中
- `[x]` 强制 Win+V 组合消费后不触发系统开始菜单
- `[x]` 已支持默认跟随 Windows 首选 UI 语言
- `[x]` 设置页、历史窗口和托盘可见文案集中到语言包
- `[x]` 增加语言回退到英文
- `[ ]` 增加长文本、CJK 字体回退和 RTL 文本布局检查
- `[x]` 完善设置页、历史窗口和输入控件的深色主题颜色
- `[x]` 增加高对比度模式下使用系统颜色的绘制路径
- `[x]` 增加深浅主题切换的短时缓动颜色过渡，不保留常驻截图缓存
- `[ ]` 增加 DPI 125%、150%、200% 测试
- `[ ]` 增加英文、简体中文、日文和韩文资源验证

完成记录：

- 当前结果：英文/简体中文和黑白主题已经覆盖核心窗口。
- 当前结果：英文/简体中文基础界面、系统语言自动选择和英文回退已完成。
- 本次完成：主题切换使用约 180ms 的平滑颜色插值，设置页和历史弹窗共用窗口定时器驱动，动画结束后才更新 DWM 边框。
- 本次完成：开关同时处理 `BN_CLICKED` 和 `BN_DOUBLECLICKED`，下拉框按当前动画目标反向，快速重复点击不会吞掉第二次交互。
- 本次完成：开关控件收紧为 `36x20` 轨道尺寸，不再保留额外外层区域，承载背景与卡片融合。
- 本次完成：主题切换期间父窗口与开关控件共用固定动画帧进度，避免承载区出现矩形轮廓；轨道两端使用高度一半的胶囊圆角。
- 待完成：启动错误提示统一纳入语言包、DPI 和更多多语言资源验证。

## 阶段十一：稳定性、隐私和安全

目标：长期后台运行不泄漏内存，不误存敏感内容，不因异常剪贴板数据崩溃。

- `[x]` 连续复制 10000 次测试
- `[x]` 连续打开关闭历史窗口 1000 次测试
- `[ ]` 连续搜索、删除、分类和粘贴测试
- `[ ]` 强制终止后重启恢复测试
- `[x]` 模拟 history.bin 截断、尾部校验损坏和临时文件残留
- `[ ]` 测试剪贴板被锁定、格式句柄失效和其他程序快速改写
- `[x]` 检查所有临时缓冲区是否在操作结束后释放
- `[ ]` 检查 GDI、USER、文件句柄和钩子数量是否稳定
- `[x]` 增加敏感内容暂停、过期和一键清理测试
- `[x]` 可选使用 DPAPI 加密历史内容
- `[x]` 日志不记录剪贴板正文
- `[ ]` 评估 UAC、管理员进程、远程桌面和多用户环境

完成记录：

- 当前结果：基础存储测试通过。
- 待完成：长期运行、异常数据、隐私和权限测试。

## 阶段十二：性能验收

目标：以实际测量而不是估算确认低内存目标。

验收目标：

- 后台空闲 Private Bytes：目标不超过 `3 MB`
- 后台空闲 Working Set：目标不超过 `10 MB`
- 打开纯文本历史窗口 Working Set：目标不超过 `16 MB`
- 图片历史窗口 Working Set：目标不超过 `32 MB`
- Release 主程序：目标不超过 `1 MB`
- 启动后不读取所有历史正文
- 启动后不解码历史图片
- 搜索和清理结束后临时内存回落
- 1000 条文本记录下窗口打开和搜索保持可用

执行项目：

- `[x]` 测量后台 Private Bytes
- `[x]` 测量后台 Working Set
- `[x]` 测量 Release 文件体积
- `[x]` 测量打开历史窗口资源
- `[ ]` 测量图片预览资源
- `[x]` 测量 1000 条文本历史
- `[x]` 测量 10000 次复制压力脚本的完成时间
- `[ ]` 记录不同 Windows 版本和 DPI 下的数据

完成记录：

- 当前 x64 实测：设置窗口 Private Bytes 约 `2.35 MB`、Working Set 约 `16.64 MB`、程序约 `233.5 KB`。
- 历史窗口实测：Private Bytes 约 `2.43 MB`、Working Set 约 `17.40 MB`。
- 10000 条记录追加、重启读取和清理测试通过，耗时约 `4.5 秒`。
- 最终资源测量：进程启动约 `74.84 ms`，设置窗口 Working Set 约 `14.23 MB`，Private Bytes 约 `2.13 MB`，x64 程序约 `240.5 KB`。
- 最终窗口压力：1000 次打开/关闭历史窗口通过。
- 新增并实际执行 `tools/stress-clipboard.ps1`：10000 次文本捕获完成约 `158122 ms`，结束后恢复原文本剪贴板。
- 本轮资源测量：Private Bytes `2.37 MB`、Working Set `18.86 MB`、句柄 `168`、GDI `22`、USER `39`。
- 1000 次历史窗口打开/关闭压力测试通过；随后资源测量为 Private Bytes `2.27 MB`、Working Set `18.76 MB`、句柄 `168`、GDI `22`、USER `39`。
- 待完成：图片预览专项测量和不同 Windows/DPI 环境数据。

## 阶段十三：发布和文档

目标：生成用户可直接运行的 Windows 便携版，并提供清晰的使用和卸载说明。

- `[x]` 增加应用图标
- `[x]` 增加 Windows 版本资源
- `[x]` 生成 x64 Release 便携版
- `[x]` 增加启动参数或调试日志开关
- `[x]` 编写安装/便携运行说明
- `[x]` 编写数据目录和清理说明
- `[x]` 编写权限、Win+V 替换和粘贴限制说明
- `[x]` 编写隐私说明
- `[ ]` 验证干净 Windows 环境运行
- `[ ]` 验证 Windows Defender 和 SmartScreen 基础行为
- `[x]` 生成 SHA-256 校验值
- `[x]` 完成当前版本体积、启动时间、内存和功能报告

完成记录：

- 当前结果：已有基础 README 和 Release 可执行文件。
- 本次完成：数据目录、清理、DPAPI、权限限制、隐私说明、x64 便携包和 SHA-256 已完成。
- 待完成：干净 Windows 环境和 Defender/SmartScreen 验证。

## 每次开发完成后的固定流程

1. 修改对应源码和测试。
2. 更新本文件中对应任务状态。
3. 在对应阶段的“完成记录”中写入实际结果。
4. 重新配置或构建受影响目标。
5. 运行相关单元测试和 CTest。
6. 对功能改动做实际运行验证。
7. 对性能相关改动重新记录 Private Bytes、Working Set 和文件体积。
8. 检查 `build/` 和用户数据没有进入源码提交范围。
9. 再开始下一个阶段。

## 当前下一步

1. 在不同 Windows 版本、DPI、多显示器、管理员窗口和安全桌面环境完成外部验证。
2. 评估异步搜索、倒排索引和后台低优先级整理是否值得增加线程复杂度。
3. 完善统一翻译表、更多语言资源和长文本布局检查。
4. 在干净 Windows 环境完成最终发布验证，并检查 Defender/SmartScreen 行为。
