# ClipLite 技术改造计划

## 一、最终目标

ClipLite 面向 Windows 10 和 Windows 11，最低运行版本为 Windows 10 1903，使用 x64 原生 Win32 架构。

目标优先级：

1. 剪贴板数据可靠、不丢失、不误存敏感内容。
2. DirectWrite 文本质量、DPI 适配和界面抗锯齿达到现代 Windows 工具水平。
3. 后台低内存、低 CPU，窗口关闭后资源及时释放。
4. Win+V、Alt+V、窗口焦点和多显示器交互稳定。
5. 保持小体积、可移植、易诊断和易恢复。

## 二、技术决策

### 平台

- 最低版本：Windows 10 1903。
- 主要验证：Windows 10 22H2、Windows 11。
- 不再维护 Windows 7、Windows 8 和 Windows 8.1 兼容路径。
- 仅发布 x64。

### 图形和文字

- Direct2D 1.1：图形、路径、圆角、边框、图标、阴影、透明混合和图片缩放。
- DirectWrite：所有自绘文字、文本布局、字体回退、CJK 字体和高 DPI 文本。
- WIC：图片解码、格式转换和缩略图输入。
- WARP：Direct2D 硬件路径不可用时的系统软件渲染回退。
- Win32：窗口、菜单、托盘、原生输入控件、文件对话框和消息循环。
- 不引入 WebView、Electron、WinUI、XAML、Qt、SQLite 或 DirectComposition。

### 数据和后台

- 保留追加式二进制历史格式和按需读取策略。
- 保留 CRC、截断恢复、原子重建、DPAPI 和来源记录。
- 单实例互斥量必须在打开历史文件之前创建。
- 剪贴板监听继续使用 `AddClipboardFormatListener`。
- 后台不轮询，不保存完整历史正文，不创建无界写入队列。

## 三、当前基线

回退节点已提交并推送到 `origin/main`：

```text
f506adfb7bd7045981c97e60882f2bfa8e36a99c
完善设置存储与快捷键生命周期
```

当前已具备：

- 原生 Win32 后台进程、托盘、单实例和启动参数。
- 文本、HTML、文件列表、DIB/DIBV5 捕获与恢复。
- 追加式历史、CRC、重建、去重、搜索、分类、置顶和删除。
- 自定义数据目录、保留策略、空间限制、DPAPI 和敏感内容过期。
- 设置窗口、历史窗口、主题、语言、剪贴板来源和快捷键配置。
- Release、CTest、窗口压力和存储压力测试基础设施。

当前主要问题：

- 自绘界面仍未统一到 Direct2D/DirectWrite。
- 渲染、窗口、设置和快捷键逻辑集中在 `src/main.cpp`。
- Win+V 拦截器需要独立重构和高权限边界测试。
- GPU、DPI、设备失效、图片资源和文本缓存尚未形成统一生命周期。

## 四、改造阶段

### 阶段 0：基线冻结和测量

状态：`[x]` 已建立回退节点。

- 记录提交 `f506adf` 作为迁移前回退点。
- 固定 Release、CTest、窗口压力和存储压力命令。
- 增加 Private Bytes、Working Set、Commit、GPU Dedicated Memory、GPU Shared Memory 和 CPU 时间记录。
- 建立设置窗口、文本历史、图片历史、主题切换和 DPI 切换的基线截图。

完成条件：任何迁移结果都能与 `f506adf` 对比并回退。

### 阶段 1：平台和构建基线

状态：`[~]` 进行中。

- 设置 Windows 10 1903 SDK 和运行版本宏。
- 移除 Windows 7/8/8.1 专用兼容分支和旧 DPI 回退逻辑。
- 动态加载只保留确有必要的可选 API。
- 完善资源文件、版本信息、x64 发布和安装器验证。
- 确认 Direct2D、DirectWrite、WIC、DWM 和 DPAPI 链接及运行时可用。

完成条件：Windows 10 1903、Windows 10 22H2、Windows 11 均可启动、安装和卸载。

### 阶段 2：渲染核心

状态：`[~]` 核心实现已完成，旧源码清理和多版本验收进行中。

- 新建 `render_context` 模块。
- 共享 Direct2D Factory 和 DirectWrite Factory。
- 建立每个窗口独立的 RenderTarget、DeviceContext、Brush、Bitmap 和文本资源。
- 实现 `BeginDraw`、`EndDraw`、设备失效重建、DPI 变化和窗口尺寸变化处理。
- 统一颜色、圆角、路径、阴影、图标和图片资源 API。
- 实现 WARP 软件渲染回退。
- 删除无调用的旧渲染实现，禁止形成两套长期维护路径。

完成条件：空窗口、设置窗口和历史窗口可以独立绘制，设备重建后不崩溃、不出现黑屏或资源泄漏。

### 阶段 3：DirectWrite 文本迁移

状态：`[~]` 核心窗口已完成，原生控件和多 DPI 实机验收进行中。

- 替换设置页、历史窗口、托盘菜单辅助绘制和状态文本。
- 统一字体族、字号、字重、行高、字距和文本方向。
- 支持 CJK 字体回退、长文本裁剪、换行和高对比度。
- 区分不透明背景 ClearType 和透明背景灰度抗锯齿。
- 增加英文、简体中文、长文本和 125%/150%/200% DPI 截图测试。

完成条件：文字不截断、不重叠，中文和英文在三档 DPI 下布局稳定。

### 阶段 4：Direct2D 图形和图片迁移

状态：`[~]` 进行中。

- 迁移历史窗口和设置页的圆角、边框、开关、图标、选中态和 hover 态。
- 使用几何路径绘制置顶、关闭、搜索和导航图标。
- 使用 WIC + Direct2D 绘制可视图片缩略图。
- 建立有上限的缩略图缓存，窗口关闭时释放窗口资源。
- 验证深色、浅色、高对比度、主题动画和多显示器切换。

完成条件：所有用户可见自绘元素无明显锯齿，关闭窗口后资源回落，图片滚动不持续增长内存。

### 阶段 5：窗口和模块拆分

状态：`[~]` 进行中。

拆分 `src/main.cpp`：

- `app_lifecycle`：进程启动、单实例、托盘、退出和诊断。
- `clipboard_monitor`：剪贴板捕获、格式转换、来源和过滤。
- `hotkey_manager`：普通快捷键、Win+V 钩子和状态清理。
- `render_context`：Direct2D、DirectWrite、WIC 和资源生命周期。
- `history_window`：历史列表、筛选、搜索、置顶和粘贴。
- `settings_window`：设置布局、控件同步、主题和语言。
- `clip_store`：历史文件、索引、重建、加密和清理。

完成条件：模块之间使用明确接口，窗口资源不再由不相关窗口创建或释放。

### 阶段 6：Win+V 和权限边界

状态：`[~]` 进行中。

- 参考 `release.zip` 的独立 `WH_KEYBOARD_LL` 拦截器结构。
- 钩子不依赖设置窗口前台状态。
- 明确处理 Win+V、普通 Win、Win+其他组合、重复按键、异常释放和进程退出。
- 普通快捷键继续使用 `RegisterHotKey`。
- 设置页显示钩子安装状态、冲突状态和回退快捷键。
- 验证普通窗口、管理员窗口、任务管理器、UAC、远程桌面和安全桌面。
- 不通过注册表重映射按键，不自动提权。

完成条件：普通桌面和常用应用中 Win+V 稳定，任何失败都不会吞掉 Win、Alt、Ctrl、Shift 或普通字符键。

### 阶段 7：存储和性能优化

状态：`[ ]` 未开始。

- 保持完整正文按需读取和图片延迟解码。
- 评估单一低优先级 I/O 线程，队列必须有上限并具备失败反馈。
- 搜索和重建操作不得长期阻塞 UI 消息循环。
- 清理、删除、重加密和目录迁移必须可取消或有明确状态。
- 重新测量文本、图片、设置、主题和长时间运行场景。

完成条件：后台资源稳定，窗口关闭后内存和句柄回落，10,000 条记录可持续操作。

### 阶段 8：用户体验和发布

状态：`[ ]` 未开始。

- 设置页提供渲染状态、Win+V 模式、权限限制和恢复方式说明。
- 启动失败使用单一可操作提示，不允许重复弹窗刷屏。
- 完善高对比度、键盘导航、DPI、多显示器和语言回退。
- 重新生成便携包、安装包、校验文件和隐私说明。
- 在 Windows 10 1903、Windows 10 22H2、Windows 11 干净环境验证。

完成条件：安装、升级、卸载、数据迁移、快捷键、历史和渲染功能通过发布验收。

## 五、性能验收预算

以下是迁移后的目标，不是当前实测结果：

| 场景 | Private Bytes | Working Set |
| --- | ---: | ---: |
| 后台空闲 | <= 4 MB | <= 12 MB |
| 设置或文本历史窗口 | <= 8 MB | <= 24 MB |
| 图片预览短时峰值 | <= 16 MB | <= 48 MB |

同时记录 GPU Dedicated Memory、GPU Shared Memory、Commit、CPU 时间、句柄、USER 和渲染帧耗时。窗口关闭后资源应回到后台基线附近。

## 六、固定验证流程

1. 修改对应模块和测试。
2. 更新本文件的阶段状态和完成记录。
3. 构建 Release 和测试目标。
4. 运行 CTest、窗口压力和存储压力测试。
5. 进行真实窗口、DPI、主题、权限和多显示器验证。
6. 性能改动重新执行资源测量。
7. 检查构建目录、用户数据和日志没有进入提交范围。
8. 更新 README 和 CHANGELOG。

## 七、当前下一步

1. 继续拆分设置和历史消息处理，减少 `main.cpp` 的消息分发职责。
2. 将原生控件的自定义绘制和语言下拉改为 DirectWrite/Direct2D 或恢复为系统控件。
3. 增加 WIC 图片格式、设备丢失、125%/150%/200% DPI 和高对比度实机验证。
4. 在管理员窗口、任务管理器、远程桌面和安全桌面记录 Win+V 边界结果。

## 八、本轮完成记录

- 已新增 `app_lifecycle`、`clipboard_monitor`、`hotkey_manager`、`render_context`、`history_window` 和 `settings_window` 模块。
- 设置窗口和历史窗口主背景、卡片、筛选条、历史文字和统计文字改由 Direct2D/DirectWrite 绘制；DPI、尺寸变化和设备目标重建进入窗口级上下文。
- WIC 工厂共享，24/32 位 DIB 和常见 DIBV5 图片仅在可见历史行按需复制、缩放和绘制，不建立常驻图片缓存。
- GDI+、旧双缓冲主绘制函数和迁移期间的不可编译旧绘制源码已删除，目标仅保留 Direct2D/DirectWrite 主绘制路径。
- Win+V 低级钩子已移入独立模块，异常超时、重复 Win 键、注入事件、普通 Win 组合和卸载状态均由模块清理。
- Release 构建、CTest、窗口压力 `100/100` 和剪贴板压力 `10000/10000` 通过。
- 当前资源测量：启动设置场景 Working Set `37.0 MB`、Private Bytes `25.88 MB`、Commit `25.88 MB`、CPU `578.12 ms`、句柄 `320`、GDI `26`、USER `57`；GPU Dedicated/Shared 计数器在当前环境未返回数据。尚未达到计划预算，需继续优化并在目标 Windows 版本复测。
